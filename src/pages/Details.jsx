import React, { useEffect, useState } from "react";

const Details = () => {
    useEffect(() => {
        window.scrollTo(0, 0);
    }, []);

    return (
        <div>Details</div>
    );
};

export default Details;